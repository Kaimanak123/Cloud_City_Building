// ===== API configuration =====
// When you deploy the backend (see /backend folder), set this to its live URL.
// e.g. 'https://api.cloudcitybuilding.com' or 'https://your-app.onrender.com'
// Leave as '' while testing locally with `npm start` in /backend (same-origin).
const API_BASE = '';
